import { ProductPagedListViewModel } from './product-paged-list-view-model';

describe('ProductPagedListViewModel', () => {
  it('should create an instance', () => {
    expect(new ProductPagedListViewModel()).toBeTruthy();
  });
});
